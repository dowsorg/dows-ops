springdoc:
  api-docs:
    enabled: false
    path: /api-docs
  swagger-ui:
    #自定义swagger前端请求路径，输入http：127.0.0.1:8080/doc会自动重定向到swagger-ui.html页面
    path: /doc
    packages-to-scan: org.dows.dxz.api
  group-configs:
    - group: dxz-admin
      packagesToScan: org.dows.dxz.rest.base
    - group: dxz-tenant
      packagesToScan: org.dows.dxz.rest.tenant
    - group: dxz-user
      packagesToScan: org.dows.dxz.rest.user
---
#本地测试环境
spring:
  config:
    activate:
      on-profile: lte
springdoc:
  api-docs:
    enabled: true
---
#开发环境
spring:
  config:
    activate:
      on-profile: dev
springdoc:
  api-docs:
    enabled: true
---
#测试环境
spring:
  config:
    activate:
      on-profile: sit
springdoc:
  api-docs:
    enabled: true

---
#生产环境
spring:
  config:
    activate:
      on-profile: uat
springdoc:
  api-docs:
    enabled: true


---
#本地环境
spring:
  config:
    activate:
      on-profile: prd
springdoc:
  api-docs:
    enabled: true